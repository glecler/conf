Personal terminal and editor configuration snapshots.

Contents:
- `nvim/`: current Neovim Lua config using lazy.nvim.
- `kitty/`: current kitty config and the selected vendored `Ciapre` theme.
- `kitty.conf`: root-level copy of the current kitty config for legacy installs.

Live sources copied from:
- `~/.config/nvim`
- `~/.config/kitty`
